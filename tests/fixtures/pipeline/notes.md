# Pipeline Test Notes

## Overview

This fixture directory provides sample files for testing
the ChunkHound indexing pipeline.

## File Types

- `main.py` — Python functions and classes
- `lib.rs` — Rust functions and struct with methods
- `settings.json` — structured JSON configuration
- `notes.md` — Markdown text (test fallback)
- `empty.py` — edge case